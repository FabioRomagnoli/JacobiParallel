LIBRARY_NAME=muparser

MUPARSER_LIBDIR=$(PACS_ROOT)/lib
MUPARSER_INCLUDE=$(PACS_ROOT)/include

#UTIL_INCLUDE=$(PACS_ROOT)/src/Parallel/Utilities

STATIC_LIBFILE=lib$(LIBRARY_NAME).a
DYNAMIC_LIBFILE=lib$(LIBRARY_NAME).so

LDLIBS+= -L$(MUPARSER_LIBDIR) $(LDFLAGS) -l$(LIBRARY_NAME)
